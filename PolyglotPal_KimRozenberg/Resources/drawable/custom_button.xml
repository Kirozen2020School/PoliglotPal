<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background" android:drawable="#00C853"/> <!-- Green color -->
    <item android:id="@android:id/pressed">
        <shape android:shape="rectangle">
            <solid android:color="#00E676"/> <!-- Lighter green when pressed -->
        </shape>
    </item>
</layer-list>
